apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ .Chart.Name }}-monitoring
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "laa-certificated-fee-scheme-api.labels" . | nindent 4 }}
spec:
  podSelector:
    matchLabels:
      {{- include "laa-certificated-fee-scheme-api.selectorLabels" . | nindent 6 }}
  policyTypes:
    - Ingress
  ingress:
    - from:
        - namespaceSelector:
            matchLabels:
              component: monitoring
